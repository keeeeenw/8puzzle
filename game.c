#include <stdio.h>

void fillboard(int *board, int dim){
	int i, j;
	for(i=0; i<dim; i++){
		for(j=0; j<dim; j++){
			board[i*dim+j] = rand
		}
	}
}

int main(void){
	int n;
	int *board;
	board = (int*)malloc(our_number_of_people * sizeof(int));
	srand(time(NULL));
}